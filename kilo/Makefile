all:
	cat Makefile

install:
	# Kilo Code CLI: Advanced terminal-based AI assistant
	# Using bun for faster global installation
	bun install -g @kilocode/cli

connect:
	# Authenticate and connect to the Kilo Gateway
	kilo /connect

run:
	# Start the Kilo interactive CLI
	kilo
