all:
	cat Makefile

install:
	# metaswarm: Multi-agent task runner and orchestrator
	# Detects environment and installs for available CLIs (Claude, Gemini, Codex)
	npx metaswarm init

setup:
	# Configure agents for the specific codebase
	npx metaswarm setup
