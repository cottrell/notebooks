from setuptools import setup

setup(name='lib_get_data',
      version='0.1',
      description='ultra basic data getter utils (for web sources mostly)',
      packages=['lib_get_data'],
      zip_safe=False)
